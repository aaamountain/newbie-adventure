# STM32按键控制LED跑马灯项目

## 项目功能
本项目实现了通过按键控制LED工作模式切换，并通过串口实现双向通信的功能。

### 主要功能
1. **按键控制LED模式**：
   - KEY0按键：LED0点亮，LED1熄灭
   - KEY2按键：LED1点亮，LED0熄灭
   - KEY1按键：切换暂停/继续状态

2. **串口通信**：
   - 实时上报当前灯光状态
   - 接收上位机"P"指令控制暂停/开启

## 硬件连接
### LED连接
- LED0：PC13 (低电平点亮)
- LED1：PC14 (低电平点亮)

### 按键连接
- KEY0：PA0 (按下为低电平，内部上拉)
- KEY1：PA1 (按下为低电平，内部上拉)
- KEY2：PA2 (按下为低电平，内部上拉)

### 串口连接
- USART1_TX：PA9
- USART1_RX：PA10
- 波特率：115200

## 文件结构
```
├── main.c              # 主程序文件
├── gpio.h/gpio.c       # GPIO配置文件
├── usart.h/usart.c     # 串口配置文件
├── delay.h/delay.c     # 延时函数文件
├── stm32f10x_it.h/c    # 中断服务函数文件
├── stm32f10x_conf.h    # 库配置文件
├── startup_stm32f10x_md.s  # 启动文件
└── LED_Control.uvprojx # Keil5项目文件
```

## 使用方法
1. 使用Keil5打开`LED_Control.uvprojx`项目文件
2. 编译项目
3. 下载到STM32F103C8T6开发板
4. 连接串口调试助手（115200波特率）
5. 按下按键测试功能

## 串口指令
- 发送"P"或"p"：切换暂停/继续状态
- 系统会实时上报LED状态信息

## 状态说明
- "状态：LED0亮，LED1灭"
- "状态：LED0灭，LED1亮" 
- "状态：暂停"
- "收到P指令：系统暂停/继续"

## 注意事项
1. 确保STM32F103C8T6的硬件连接正确
2. 需要安装STM32F1xx标准外设库
3. 按键采用软件消抖，消抖时间为50ms
4. LED为低电平点亮方式